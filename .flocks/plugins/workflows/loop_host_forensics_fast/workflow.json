{
  "id": "loop_host_forensics_fast",
  "name": "loop_host_forensics_fast",
  "description": "从文件或 inputs 读取主机列表；可选 ssh_user 指定 SSH 用户（ssh_run_script 使用 user@host），省略则仅主机标识（Agent 默认 root）。循环调用 host-forensics-fast，结果落盘并生成总结。",
  "start": "init_hosts",
  "nodes": [
    {
      "id": "init_hosts",
      "type": "python",
      "name": "初始化主机列表与日志",
      "description": "解析 hosts_file / hosts，去重；读取可选 ssh_user 并透传；写日志头；设置循环变量。",
      "code": "import datetime\nimport os\nfrom pathlib import Path\nfrom flocks.workspace.manager import WorkspaceManager\n\nhosts = inputs.get(\"hosts\")\nif hosts is None:\n    hosts = []\nif isinstance(hosts, str):\n    hosts = [hosts.strip()] if str(hosts).strip() else []\nelif not isinstance(hosts, list):\n    hosts = []\n\nhosts_file = str(inputs.get(\"hosts_file\") or \"\").strip()\nif hosts_file:\n    p = Path(hosts_file).expanduser()\n    if p.is_file():\n        for line in p.read_text(encoding=\"utf-8\").splitlines():\n            line = line.strip()\n            if not line or line.startswith(\"#\"):\n                continue\n            hosts.append(line)\n\nseen = set()\nuniq = []\nfor h in hosts:\n    hs = str(h).strip()\n    if not hs or hs in seen:\n        continue\n    seen.add(hs)\n    uniq.append(hs)\nhosts = uniq\n\nsu = inputs.get(\"ssh_user\")\nif isinstance(su, str):\n    su = su.strip()\nelse:\n    su = \"\"\n\nws = WorkspaceManager.get_instance()\noday = datetime.date.today().isoformat()\noutput_dir = str(ws.get_workspace_dir() / \"outputs\" / oday)\nos.makedirs(output_dir, exist_ok=True)\nbatch_report_path = os.path.join(output_dir, \"batch_host_triage_log.md\")\n\noutputs[\"hosts\"] = hosts\noutputs[\"ssh_user\"] = su\noutputs[\"host_idx\"] = 0\noutputs[\"triage_results\"] = []\noutputs[\"batch_report_path\"] = batch_report_path\noutputs[\"should_continue\"] = \"continue\" if hosts else \"exit\"\n\nif hosts:\n    header = (\n        \"# 批量主机快速巡检日志\\n\\n\"\n        \"- 开始日期: \" + oday + \"\\n\"\n        \"- 主机数量: \" + str(len(hosts)) + \"\\n\"\n    )\n    if su:\n        header += \"- SSH 用户: `\" + su + \"`（`ssh_run_script` 的 host 使用 `user@<目标>`）\\n\"\n    else:\n        header += \"- SSH 用户: （未指定 `ssh_user`，仅下列主机标识、Agent/工具默认账户，一般为 root）\\n\"\n    header += \"\\n---\\n\\n\"\n    with open(batch_report_path, \"w\", encoding=\"utf-8\") as f:\n        f.write(header)\n"
    },
    {
      "id": "loop_check",
      "type": "loop",
      "name": "循环判断",
      "select_key": "should_continue",
      "description": "should_continue 为 continue 则巡检下一台，为 exit 则结束循环"
    },
    {
      "id": "inspect_host",
      "type": "python",
      "name": "单台快速巡检",
      "description": "Tool: task(host-forensics-fast)。若提供 ssh_user 则要求 ssh_run_script 使用 user@host；否则裸 host（默认 root）。列表项已含 user@ 时不重复拼接。",
      "code": "hosts = inputs.get(\"hosts\", [])\nidx = int(inputs.get(\"host_idx\", 0))\nhost = hosts[idx] if 0 <= idx < len(hosts) else \"\"\nhost = str(host).strip()\n\nsu = inputs.get(\"ssh_user\")\nif isinstance(su, str):\n    su = su.strip()\nelse:\n    su = \"\"\n\nif \"@\" in host:\n    ssh_target = host\n    user_hint = (\n        \"主机列表项已含 `user@host` 形式，请直接使用下列 host 参数，勿再拼接用户。\"\n    )\nelif su:\n    ssh_target = su + \"@\" + host\n    user_hint = (\n        \"工作流已指定 `ssh_user`=`\" + su + \"`。`ssh_run_script` 的 host 参数必须使用 `user@host`：\"\n        + ssh_target + \"。\"\n    )\nelse:\n    ssh_target = host\n    user_hint = (\n        \"未指定 `ssh_user`，请使用下列裸主机标识连接（Agent/工具默认 SSH 用户，一般为 root）：\"\n        + ssh_target + \"。\"\n    )\n\nidx1 = idx + 1\ndesc = \"Fast triage item \" + str(idx1)\nprompt = (\n    \"你是 host-forensics-fast 工作模式：对下列目标执行 Linux 主机快速安全巡检（首轮研判）。\\n\"\n    \"请使用 ssh_run_script，script_path 为 `.flocks/plugins/agents/host-forensics-fast/scripts/triage_fast.sh`。\\n\"\n    + user_hint\n    + \"\\n\\n本次 `ssh_run_script` 的 host 参数必须使用：\\n\"\n    + ssh_target\n    + \"\\n\\n请输出简洁 Markdown：结论、可疑项、风险判断、后续建议。\"\n)\nres = tool.run_safe(\n    \"task\",\n    description=desc,\n    prompt=prompt,\n    subagent_type=\"host-forensics-fast\",\n    run_in_background=False,\n)\ntext = res.get(\"text\") or \"\"\nok = bool(res.get(\"success\"))\nerr = res.get(\"error\") or \"\"\n\ntr = inputs.get(\"triage_results\", [])\ntr = list(tr) if isinstance(tr, list) else []\ntr.append(\n    {\n        \"host\": host,\n        \"ssh_user\": su,\n        \"ssh_target\": ssh_target,\n        \"success\": ok,\n        \"text\": text,\n        \"error\": err,\n    }\n)\noutputs[\"triage_results\"] = tr\n\nbatch_report_path = inputs.get(\"batch_report_path\", \"\")\nsection = \"\\n## 主机: \" + ssh_target + \"\\n\\n\"\nif ok:\n    section += text + \"\\n\\n\"\nelse:\n    section += \"**调用失败**: \" + err + \"\\n\\n\"\nsection += \"---\\n\"\nif batch_report_path:\n    with open(batch_report_path, \"a\", encoding=\"utf-8\") as f:\n        f.write(section)\n\noutputs[\"last_host\"] = host\noutputs[\"last_ssh_target\"] = ssh_target\noutputs[\"last_success\"] = ok\n"
    },
    {
      "id": "advance_index",
      "type": "python",
      "name": "下一台主机",
      "description": "host_idx 自增并更新 should_continue",
      "code": "hosts = inputs.get(\"hosts\", [])\nidx = int(inputs.get(\"host_idx\", 0)) + 1\noutputs[\"host_idx\"] = idx\noutputs[\"should_continue\"] = \"continue\" if idx < len(hosts) else \"exit\"\n"
    },
    {
      "id": "finalize_summary",
      "type": "python",
      "name": "总结巡检结果",
      "description": "LLM 总结写入 batch_host_triage_summary.md；同时将完整 triage_results 写入 batch_host_triage_results.json，及逐台完整正文 batch_host_triage_raw_all.md。",
      "code": "import datetime\nimport json\nimport os\nfrom flocks.workspace.manager import WorkspaceManager\n\nresults = inputs.get(\"triage_results\") or []\nif not isinstance(results, list):\n    results = []\n\nws = WorkspaceManager.get_instance()\noday = datetime.date.today().isoformat()\noutput_dir = str(ws.get_workspace_dir() / \"outputs\" / oday)\nos.makedirs(output_dir, exist_ok=True)\nsummary_path = os.path.join(output_dir, \"batch_host_triage_summary.md\")\nresults_json_path = os.path.join(output_dir, \"batch_host_triage_results.json\")\nraw_all_path = os.path.join(output_dir, \"batch_host_triage_raw_all.md\")\nbatch_report_path = inputs.get(\"batch_report_path\", \"\")\nhosts = inputs.get(\"hosts\", [])\nif not isinstance(hosts, list):\n    hosts = []\n\nsu = inputs.get(\"ssh_user\")\nif isinstance(su, str):\n    su = su.strip()\nelse:\n    su = \"\"\n\nif not results:\n    summary = (\n        \"# 批量主机快速巡检总结\\n\\n\"\n        \"未提供任何可巡检主机（`hosts` 为空且未从 `hosts_file` 读取到有效行）。\\n\"\n    )\nelse:\n    lines = []\n    for r in results:\n        if not isinstance(r, dict):\n            continue\n        label = r.get(\"ssh_target\") or r.get(\"host\", \"\")\n        ok = r.get(\"success\", False)\n        lines.append(\"- \" + str(label) + \": \" + (\"成功\" if ok else \"失败\"))\n    brief = \"共巡检 \" + str(len(results)) + \" 台主机。\\n\" + \"\\n\".join(lines)\n    prompt = (\n        \"以下是批量 Linux 主机快速巡检的逐台结果。请用中文输出 Markdown 报告，包含：\\n\"\n        \"## 执行摘要（2-4 句）\\n\"\n        \"## 逐台要点（成功则概括结论与可疑项；失败则写原因）\\n\"\n        \"## 整体风险评估与后续建议\\n\\n\"\n        \"逐台状态：\\n\" + brief + \"\\n\\n详细输出（每台正文已截断）：\\n\"\n    )\n    for r in results:\n        if isinstance(r, dict):\n            body = str(r.get(\"text\", \"\"))[:4000]\n            lab = r.get(\"ssh_target\") or r.get(\"host\", \"\")\n            prompt += \"\\n### \" + str(lab) + \"\\n\" + body + \"\\n\"\n    summary = llm.ask(prompt, timeout_s=300, max_retries=2, retry_delay_s=2)\n\nwith open(summary_path, \"w\", encoding=\"utf-8\") as f:\n    f.write(summary)\n\nbundle = {\n    \"date\": oday,\n    \"hosts\": hosts,\n    \"ssh_user\": su,\n    \"batch_report_path\": batch_report_path,\n    \"summary_path\": summary_path,\n    \"triage_results\": results,\n}\nwith open(results_json_path, \"w\", encoding=\"utf-8\") as f:\n    json.dump(bundle, f, ensure_ascii=False, indent=2)\n\nparts = [\"# 批量巡检原始逐台输出（完整未截断）\\n\\n\"]\nfor r in results:\n    if not isinstance(r, dict):\n        continue\n    lab = r.get(\"ssh_target\") or r.get(\"host\", \"\")\n    parts.append(\"## \" + str(lab) + \"\\n\\n\")\n    parts.append(\"- success: \" + str(r.get(\"success\")) + \"\\n\")\n    u = r.get(\"ssh_user\")\n    if isinstance(u, str) and u.strip():\n        parts.append(\"- ssh_user: \" + u + \"\\n\")\n    err = r.get(\"error\") or \"\"\n    if err:\n        parts.append(\"- error:\\n\\n```\\n\" + str(err) + \"\\n```\\n\\n\")\n    parts.append(str(r.get(\"text\", \"\")) + \"\\n\\n---\\n\\n\")\nif not results:\n    parts.append(\"_（无逐台记录）_\\n\")\nwith open(raw_all_path, \"w\", encoding=\"utf-8\") as f:\n    f.write(\"\".join(parts))\n\noutputs[\"executive_summary\"] = summary\noutputs[\"summary_path\"] = summary_path\noutputs[\"results_json_path\"] = results_json_path\noutputs[\"raw_all_path\"] = raw_all_path\noutputs[\"batch_report_path\"] = batch_report_path\n"
    }
  ],
  "edges": [
    {
      "from": "init_hosts",
      "to": "loop_check",
      "order": 0
    },
    {
      "from": "loop_check",
      "to": "inspect_host",
      "label": "continue",
      "order": 0
    },
    {
      "from": "loop_check",
      "to": "finalize_summary",
      "label": "exit",
      "order": 1
    },
    {
      "from": "inspect_host",
      "to": "advance_index",
      "order": 0
    },
    {
      "from": "advance_index",
      "to": "loop_check",
      "order": 0
    }
  ],
      "metadata": {
    "sampleInputs": {
      "hosts": [
        "web01.example.internal",
        "db01.example.internal"
      ],
      "ssh_user": "flocks"
    }
  }
}